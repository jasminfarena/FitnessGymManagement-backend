# FitnessGymManagement-backend

Fitness Management System that is used to register new gym members and update existing members.


## 🚀 Tech Stack

- Backend: Node.js, Express
- Database: MySQL
- Authentication: JWT + bcrypt
- Environment: dotenv
- Other tools: Postman / Thunder Client for API testing

## 📄 Database Setup

Before running the frontend and backend, you need to run the SQL script in table_query.txt to create and insert the necessary data.

## 🛠️ Configure Environment Variables (.env)

- Open the .env file.
- Update the database credentials (DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, DB_PORT).
- Change the port number if needed.

### 4. Start the server

  1. Install Dependencies
       ```bash
     npm install
     ```
  2. Run the Backend
     ```bash
     npm run start
     ```
